import { SearchHighlightDirective } from './search-highlight.directive';

describe('SearchHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
